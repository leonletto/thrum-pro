interface:
  display_name: "Thrum Load Context"
  short_description: "Restore saved agent work context after compaction or session restart"
  default_prompt: "Run the thrum load-context workflow and summarize the result."
