interface:
  display_name: "Thrum Restart"
  short_description: "Save a conversation snapshot and prepare for session restart. Use when you need a fresh session due to context exhaustion, rate limits, or stuck state."
  default_prompt: "Run the thrum restart workflow and summarize the result."
